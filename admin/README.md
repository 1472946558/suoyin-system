# Admin 管理端目录说明

本目录用于承载黄金回收小程序配套的后台前端工程。当前项目口径已经明确为“微信小程序 + 独立后台管理端 + 收款方式登记 + 模板化交付”，因此后台不应塞进小程序端，而应作为单独的 Web 管理台建设。

## 当前定位

- 面向角色：老板主控台为主，店长可按权限进入受限运营台。
- 第一阶段目标：优先交付登录、首页、门店、账号、角色权限、商品、订单、回收、系统配置和模板初始化入口。
- 权限原则：前端只识别能力码和数据范围，不把权限写死在页面文案或路由判断里。

## 模板参考

现有后台前端模板线索参考：

- `/Users/xiaoliao/Desktop/openclaw/workspace/projects/fullstack-portfolio-website/frontend`

可复用的主要判断：

- 独立前端工程形态成立，适合继续沿用 `Vite + Vue 3 + TypeScript` 方案。
- 基础工程应保持轻量，先把登录、布局、导航、权限骨架和 API 分层搭起来，再逐步接入真实业务接口。
- 后台视觉可以延续现有模板的独立 Web 管理台方向，但信息密度、导航层级和权限提示要按业务后台重新设计。

## 建议首版工程骨架

首版可按以下结构落地：

```text
admin/
  README.md
  src/
    main.ts
    App.vue
    router/
    layouts/
    pages/
    components/
    api/
    permission/
    styles/
    types/
```

各层职责建议如下：

- `layouts/`：后台整体框架，包括侧边菜单、顶栏、门店切换、账号区域。
- `pages/`：页面级视图，按首页、组织权限、商品业务、系统与模板分组。
- `api/`：登录、门店、账号、角色权限、商品、订单、回收单、系统配置、打印模板等接口封装。
- `permission/`：能力码、数据范围、菜单裁剪、路由守卫。
- `types/`：角色、门店、账号、商品、订单、回收单、系统配置等领域类型。

## 第一周交付边界

第一周后台前端建议只承诺以下内容：

- 基础工程初始化。
- 登录页。
- 后台首页。
- 门店管理页骨架。
- 账号管理页骨架。
- 角色权限页骨架。
- 商品、订单、回收单和系统配置页面。
- 模板初始化入口占位。

不建议第一周就承诺：

- 完整自定义角色系统。
- 复杂报表与经营分析。
- 全量系统配置细项。
- 真实微信支付和商户参数配置页。

## 与其他模块的协作边界

- 小程序端根据能力码和数据范围决定菜单、按钮、页面访问。
- 后端负责真正的鉴权、数据隔离和高风险操作校验。
- 安全与审计要求以操作日志、权限变更留痕和系统配置变更留痕为准。

详细页面清单、菜单结构、老板最小权限管理功能和第一周优先级，请查看：

- `/Users/xiaoliao/Desktop/openclaw/workspace/projects/gold-recycle-miniapp/04_execution/ADMIN_INFORMATION_ARCHITECTURE.md`

## 当前联调模式

- 默认模式是 `strict API`：登录、加载、保存、新建全部直连真实接口，失败不会自动回退到本地数据。
- 如果只是做本地浏览，可在启动前显式设置 `VITE_ENABLE_ADMIN_LOCAL_DATA=true`。
- 即使开启本地数据，保存和新建也仍然只走真实 API，不会伪造成功。
